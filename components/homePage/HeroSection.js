import { 
  Header,
  HeroContent,
} from '../index'

function HeroSection(){
  return(
    <section className='mx-auto px-0'>
      <Header />
      <HeroContent />
    </section>
  )
}

export default HeroSection